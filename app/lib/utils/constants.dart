import 'package:flutter/material.dart';

const MaterialColor primaryColor = Colors.blue;
const MaterialColor selectedColor = Colors.purple;
const Duration sentDuration = Duration(milliseconds: 1000);
