export PATH="$PATH:/"
var=$(git status)
echo ${var:0}
if [[ ${var:0} =~ "*.h" ]];
	then
        echo 'Something to do'
	    $(make)
	else if [[ ${var:0} =~ "*.cpp" ]];
	  rm 
    else
      echo 'Nothing to do'
    
    fi

