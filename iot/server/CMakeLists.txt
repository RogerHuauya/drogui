cmake_minimum_required(VERSION 2.8)
project(server_cam)
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( server_cam server_cam.cpp)
target_link_libraries( server_cam ${OpenCV_LIBS} )